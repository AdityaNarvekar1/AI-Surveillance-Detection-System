# ─────────────────────────────────────────────────────────────────────────────
# Real-Time AI Violence & Anomaly Detection System — Dependencies
# Python 3.12+, Windows 10/11, CPU only
# ─────────────────────────────────────────────────────────────────────────────

# UI
streamlit>=1.34

# Computer vision
opencv-python-headless>=4.9

# Object detection (YOLOv8) — pulls torch automatically
ultralytics>=8.2

# Deep learning — tensorflow-cpu works on Python 3.12+ Windows
# Do NOT use plain 'tensorflow' on Windows CPU — it may try to load CUDA
tensorflow-cpu>=2.15,<2.17

# h5py for .h5 model file support
h5py>=3.8

# Data / utilities
numpy>=1.26,<2.0
pandas>=2.1
pillow>=10.0

# ─── Notes ────────────────────────────────────────────────────────────────────
# tensorflow-cpu 2.15/2.16 are the sweet spot for Python 3.12 + Windows CPU.
# numpy must stay <2.0 — TensorFlow is not yet compatible with numpy 2.x.
# If you still get a numpy conflict after install, run:
#   pip install "numpy>=1.26,<2.0" --force-reinstall